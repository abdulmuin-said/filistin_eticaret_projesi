@using FilistinProje.Core.Varliklar
@using Microsoft.AspNetCore.Identity
@inject SignInManager<AppUser> SignInManager
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@{
    var isAuthenticated = SignInManager.IsSignedIn(User);
    var isLoggedInAttr = isAuthenticated ? "true" : "false";
}
<!-- Spin the Wheel Overlay -->
<div id="spinWheelOverlay"
     data-logged-in="@isLoggedInAttr"
     class="fixed inset-0 z-[9999] hidden flex items-center justify-center bg-brand-darktext/70 backdrop-blur-sm"
     role="dialog" aria-modal="true">
    <div class="relative bg-brand-warmcream rounded-3xl shadow-2xl border border-[#c9b58a]/60 p-5 md:p-8 max-w-xs sm:max-w-sm md:max-w-md w-full mx-auto overflow-visible">

        <!-- Anti-Forgery Token for API calls -->
        @Html.AntiForgeryToken()
        <input id="wheelAntiForgeryToken" type="hidden" />

        <!-- Close Button -->
        <button type="button" class="sw-close-btn absolute -top-3 -end-3 z-10 w-9 h-9 bg-white rounded-full shadow-md border border-brand-darkborder flex items-center justify-center text-brand-mutedtext hover:bg-brand-warmcream hover:text-brand-olive transition-all">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                <path d="M6 18L18 6M6 6l12 12" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </button>

        <!-- WHEEL SECTION -->
        <div class="sw-wheel-container text-center">
            <h2 class="sw-title font-serif text-2xl md:text-3xl text-brand-olive mb-1"></h2>
            <p class="sw-subtitle text-sm text-brand-mutedtext mb-5 font-light"></p>

            <div class="relative inline-flex items-center justify-center mb-5">
                <canvas id="wheelCanvas" class="block mx-auto"></canvas>
            </div>

            <button type="button" class="sw-spin-btn w-full bg-brand-gold hover:bg-brand-olive text-white font-semibold text-sm tracking-widest uppercase py-3.5 rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"></button>

            <p class="sw-onechance text-[10px] text-brand-mutedtext mt-3"></p>
        </div>

        <!-- RESULT SECTION -->
        <div class="sw-result-modal hidden text-center">
            <div class="sw-result-icon mb-4"></div>
            <h2 class="sw-result-title font-serif text-2xl md:text-3xl text-brand-olive mb-2"></h2>
            <p class="sw-result-subtitle text-sm text-brand-mutedtext mb-5 font-light"></p>

            <!-- Coupon Code Display -->
            <div class="sw-coupon-code hidden bg-brand-olive text-brand-gold font-mono font-bold text-lg tracking-widest py-3 px-6 rounded-xl mb-5 inline-block border border-brand-gold/30 shadow-inner select-all"></div>

            <!-- Copy Button -->
            <button type="button" class="sw-copy-btn hidden w-full bg-brand-olive hover:bg-[#1c2001] text-white font-semibold text-sm tracking-widest uppercase py-2.5 rounded-xl mb-3 border border-brand-gold/30 transition-all duration-300"></button>

            <!-- Primary Action (Use Coupon / Login / Close) -->
            <button type="button" class="sw-claim-btn w-full bg-brand-gold hover:bg-brand-olive text-white font-semibold text-sm tracking-widest uppercase py-3.5 rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed"></button>

            <!-- Alt Action (Copy when not logged in) -->
            <button type="button" class="sw-claim-btn-alt hidden w-full mt-2 bg-brand-warmcream hover:bg-brand-cream text-brand-mutedtext font-semibold text-sm tracking-widest uppercase py-2.5 rounded-xl border border-brand-darkborder transition-all duration-300"></button>
        </div>

        <div class="mt-4 text-center">
            <span class="text-[9px] tracking-[0.25em] uppercase text-brand-mutedtext font-light">7ANRPS48.com</span>
        </div>
    </div>
</div>

<script src="~/js/site-spin-wheel.js" asp-append-version="true"></script>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        @if (!isAuthenticated)
        {
            <text>
            var swShown = false;
            try { swShown = localStorage.getItem('sw_shown') === 'true'; } catch(e) {}
            if (!swShown) {
                setTimeout(function () {
                    if (window.SpinWheel && typeof window.SpinWheel.open === 'function') {
                        window.SpinWheel.open();
                    }
                }, 2500);
            }
            </text>
        }
    });
</script>
