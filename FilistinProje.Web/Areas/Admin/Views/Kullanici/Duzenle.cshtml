@model FilistinProje.Core.Models.KullaniciDuzenleViewModel
@{
    ViewData["Title"] = Localizer["Admin_EditUser"];
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";

    var lastLoginText = Model.LastAdminLoginUtc.HasValue
        ? ToPalestineTime(Model.LastAdminLoginUtc.Value).ToString("dd.MM.yyyy HH:mm")
        : Localizer["Admin_NoAdminLoginYet"].Value;

    var previousLoginText = Model.PreviousAdminLoginUtc.HasValue
        ? ToPalestineTime(Model.PreviousAdminLoginUtc.Value).ToString("dd.MM.yyyy HH:mm")
        : "-";

    var displayName = string.IsNullOrWhiteSpace(Model.AdSoyad) ? Localizer["Admin_UnnamedUser"].Value : Model.AdSoyad;
    var initial = displayName.Substring(0, 1).ToUpperInvariant();
}

@functions {
    private static DateTime ToPalestineTime(DateTime utc)
    {
        var utcValue = DateTime.SpecifyKind(utc, DateTimeKind.Utc);
        foreach (var id in new[] { "Asia/Hebron", "West Bank Standard Time" })
        {
            try
            {
                return TimeZoneInfo.ConvertTimeFromUtc(utcValue, TimeZoneInfo.FindSystemTimeZoneById(id));
            }
            catch (TimeZoneNotFoundException) { }
            catch (InvalidTimeZoneException) { }
        }
        return utcValue.AddHours(3);
    }
}

<style>
    .ca-user-edit-shell {
        max-width: 1280px;
        margin: 0 auto;
    }

    .ca-user-edit-grid {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 360px;
        gap: 18px;
        align-items: start;
    }

    .ca-user-edit-hero {
        display: flex;
        justify-content: space-between;
        gap: 16px;
        align-items: flex-start;
        margin-bottom: 18px;
    }

    .ca-edit-section + .ca-edit-section {
        border-top: 1px solid var(--ca-border);
        margin-top: 24px;
        padding-top: 24px;
    }

    .ca-edit-section-title {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 16px;
        color: var(--ca-text);
        font-weight: 800;
        font-size: .96rem;
    }

    .ca-edit-section-title i {
        width: 34px;
        height: 34px;
        border-radius: 10px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: rgba(var(--ca-primary-rgb), .08);
        color: var(--ca-primary);
    }

    .ca-readonly-input {
        background: var(--ca-bg) !important;
        color: var(--ca-text-muted);
        cursor: not-allowed;
    }

    .ca-field-help {
        display: block;
        margin-top: 7px;
        color: var(--ca-text-muted);
        font-size: .78rem;
        line-height: 1.45;
    }

    .ca-role-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 12px;
    }

    .ca-role-option {
        position: relative;
        min-height: 116px;
        border: 1px solid var(--ca-border);
        border-radius: 10px;
        background: #fff;
        padding: 14px 14px 14px 42px;
        cursor: pointer;
        transition: .18s ease;
    }

    .ca-role-option:hover {
        border-color: rgba(var(--ca-primary-rgb), .35);
        box-shadow: 0 10px 24px rgba(49, 53, 17, .07);
        transform: translateY(-1px);
    }

    .ca-role-option.is-selected {
        border-color: var(--ca-primary);
        background: rgba(var(--ca-primary-rgb), .04);
    }

    .ca-role-option.is-disabled {
        cursor: not-allowed;
        opacity: .72;
    }

    .ca-role-option input {
        position: absolute;
        top: 17px;
        left: 15px;
        accent-color: var(--ca-primary);
    }

    .ca-role-option strong {
        display: block;
        margin-bottom: 6px;
        color: var(--ca-text);
        font-size: .9rem;
    }

    .ca-role-option span {
        display: block;
        color: var(--ca-text-muted);
        font-size: .78rem;
        line-height: 1.45;
    }

    .ca-user-summary {
        position: sticky;
        top: 116px;
    }

    .ca-profile-head {
        display: flex;
        gap: 14px;
        align-items: center;
        padding: 18px;
        border-bottom: 1px solid var(--ca-border);
    }

    .ca-profile-avatar-lg {
        width: 58px;
        height: 58px;
        border-radius: 18px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: var(--ca-primary);
        color: #fff;
        font-weight: 800;
        font-size: 1.35rem;
        flex: 0 0 auto;
    }

    .ca-summary-list {
        display: grid;
        gap: 12px;
    }

    .ca-summary-item {
        display: flex;
        justify-content: space-between;
        gap: 14px;
        padding: 12px 0;
        border-bottom: 1px solid var(--ca-border);
    }

    .ca-summary-item:last-child {
        border-bottom: 0;
        padding-bottom: 0;
    }

    .ca-summary-item span {
        color: var(--ca-text-muted);
        font-size: .78rem;
    }

    .ca-summary-item strong {
        color: var(--ca-text);
        font-size: .82rem;
        text-align: right;
    }

    .ca-guidance-list {
        margin: 0;
        padding: 0;
        list-style: none;
        display: grid;
        gap: 10px;
    }

    .ca-guidance-list li {
        display: flex;
        gap: 9px;
        color: var(--ca-text-muted);
        font-size: .82rem;
        line-height: 1.45;
    }

    .ca-guidance-list i {
        color: var(--ca-accent);
        margin-top: 3px;
    }

    @@media (max-width: 1100px) {
        .ca-user-edit-grid {
            grid-template-columns: 1fr;
        }

        .ca-user-summary {
            position: static;
        }
    }

    @@media (max-width: 760px) {
        .ca-user-edit-hero {
            flex-direction: column;
        }

        .ca-role-grid {
            grid-template-columns: 1fr;
        }
    }
</style>

<div class="container-fluid ca-fade-in">
    <div class="ca-user-edit-shell">
        <div class="ca-user-edit-hero">
            <div>
                <div class="ca-page-kicker">@Localizer["Admin_MemberManagement"]</div>
                <h1 class="ca-page-title">@Localizer["Admin_EditUser"]</h1>
                <p class="ca-page-subtitle">@Localizer["Admin_EditUserSubtitle"]</p>
            </div>
            <div class="d-flex gap-2 flex-wrap justify-content-end">
                <a href="/Admin/Kullanici" class="ca-btn ca-btn-outline">
                    <i class="fas fa-arrow-left"></i> @Localizer["Admin_BackToUsers"]
                </a>
                <a href="/Admin/Kullanici/SifreSifirla/@Model.Id" class="ca-btn ca-btn-outline">
                    <i class="fas fa-key"></i> @Localizer["Admin_ChangePassword"]
                </a>
            </div>
        </div>

        <div class="ca-user-edit-grid">
            <form method="post" class="ca-card">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="Email" />
                @if (Model.IsCurrentUser)
                {
                    <input type="hidden" asp-for="SelectedRole" />
                }

                <div class="ca-card-header">
                    <h2 class="ca-card-title mb-0">@Localizer["Admin_AccountInformation"]</h2>
                </div>
                <div class="ca-card-body">
                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div class="ca-alert ca-alert-danger mb-3" asp-validation-summary="All"></div>
                    }

                    @if (TempData["Hata"] != null)
                    {
                        <div class="ca-alert ca-alert-danger mb-3">
                            <i class="fas fa-triangle-exclamation"></i>
                            <span>@TempData["Hata"]</span>
                        </div>
                    }

                    @if (Model.IsCurrentUser)
                    {
                        <div class="ca-alert ca-alert-warning mb-3">
                            <i class="fas fa-shield-halved"></i>
                            <span>@Localizer["Admin_CurrentAccountRoleLocked"]</span>
                        </div>
                    }

                    <section class="ca-edit-section">
                        <div class="ca-edit-section-title">
                            <i class="fas fa-address-card"></i>
                            <span>@Localizer["Admin_BasicInformation"]</span>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="ca-label" asp-for="AdSoyad">@Localizer["Admin_Ad_Soyad"] <span class="required">*</span></label>
                                <input asp-for="AdSoyad" class="ca-input" autocomplete="name" required />
                                <span asp-validation-for="AdSoyad" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6">
                                <label class="ca-label" asp-for="Email">@Localizer["Admin_E_posta"]</label>
                                <input asp-for="Email" class="ca-input ca-readonly-input" readonly />
                                <span class="ca-field-help">@Localizer["Admin_EmailCannotBeChanged"]</span>
                            </div>

                            <div class="col-md-6">
                                <label class="ca-label" asp-for="PhoneNumber">@Localizer["Admin_Telefon"]</label>
                                <input asp-for="PhoneNumber" class="ca-input" inputmode="tel" autocomplete="tel" maxlength="20" placeholder="@Localizer["Admin_PalestinePhonePlaceholder"]" />
                                <span asp-validation-for="PhoneNumber" class="text-danger small"></span>
                            </div>

                            <div class="col-md-6">
                                <label class="ca-label" asp-for="Sehir">@Localizer["Admin_Sehir"]</label>
                                <input asp-for="Sehir" class="ca-input" autocomplete="address-level1" placeholder="@Localizer["Admin_CityPlaceholder"]" />
                                <span asp-validation-for="Sehir" class="text-danger small"></span>
                            </div>
                        </div>
                    </section>

                    <section class="ca-edit-section">
                        <div class="ca-edit-section-title">
                            <i class="fas fa-user-shield"></i>
                            <span>@Localizer["Admin_PermissionAndRole"]</span>
                        </div>

                        <div class="ca-role-grid">
                            @foreach (var option in Model.RoleOptions)
                            {
                                var isSelected = string.Equals(Model.SelectedRole, option.Value, StringComparison.OrdinalIgnoreCase);
                                var roleClass = isSelected ? "is-selected" : string.Empty;
                                var disabledClass = Model.IsCurrentUser ? "is-disabled" : string.Empty;

                                <label class="ca-role-option @roleClass @disabledClass">
                                    <input type="radio"
                                           asp-for="SelectedRole"
                                           value="@option.Value"
                                           disabled="@Model.IsCurrentUser" />
                                    <strong>@option.Label</strong>
                                    <span>@option.Description</span>
                                </label>
                            }
                        </div>

                        <span class="ca-field-help">
                            @Localizer["Admin_RoleSelectionHelp"]
                        </span>
                    </section>
                </div>

                <div class="ca-card-footer d-flex justify-content-end gap-2 flex-wrap">
                    <a href="/Admin/Kullanici" class="ca-btn ca-btn-outline">@Localizer["Admin_Iptal"]</a>
                    <button type="submit" class="ca-btn ca-btn-primary">
                        <i class="fas fa-save"></i> @Localizer["Admin_SaveChanges"]
                    </button>
                </div>
            </form>

            <aside class="ca-user-summary">
                <div class="ca-card mb-3">
                    <div class="ca-profile-head">
                        <span class="ca-profile-avatar-lg">@initial</span>
                        <div class="min-w-0">
                            <h2 class="ca-card-title mb-1">@displayName</h2>
                            <div class="small text-muted text-break">@Model.Email</div>
                            <div class="mt-2">
                                @if (Model.IsBanned)
                                {
                                    <span class="ca-badge ca-badge-danger"><i class="fas fa-ban"></i> @Localizer["Admin_LoginBlocked"]</span>
                                }
                                else
                                {
                                    <span class="ca-badge ca-badge-success"><i class="fas fa-check"></i> @Localizer["Admin_ActiveAccount"]</span>
                                }
                            </div>
                        </div>
                    </div>
                    <div class="ca-card-body">
                        <div class="ca-summary-list">
                            <div class="ca-summary-item">
                                <span>@Localizer["Admin_CurrentRole"]</span>
                                <strong>@Model.CurrentRoleLabel</strong>
                            </div>
                            <div class="ca-summary-item">
                                <span>@Localizer["Admin_RoleDescription"]</span>
                                <strong>@Model.CurrentRoleDescription</strong>
                            </div>
                            <div class="ca-summary-item">
                                <span>@Localizer["Admin_LastAdminLogin"]</span>
                                <strong>@lastLoginText</strong>
                            </div>
                            <div class="ca-summary-item">
                                <span>@Localizer["Admin_PreviousAdminLogin"]</span>
                                <strong>@previousLoginText</strong>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="ca-card">
                    <div class="ca-card-header">
                        <h2 class="ca-card-title mb-0">@Localizer["Admin_UsageNotes"]</h2>
                    </div>
                    <div class="ca-card-body">
                        <ul class="ca-guidance-list">
                            <li>
                                <i class="fas fa-circle-info"></i>
                                <span>@Localizer["Admin_EmailReadonlyNote"]</span>
                            </li>
                            <li>
                                <i class="fas fa-shield-halved"></i>
                                <span>@Localizer["Admin_MemberRoleNote"]</span>
                            </li>
                            <li>
                                <i class="fas fa-key"></i>
                                <span>@Localizer["Admin_PasswordOperationNote"]</span>
                            </li>
                        </ul>
                    </div>
                </div>
            </aside>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.querySelectorAll('.ca-role-option input[type="radio"]').forEach(function (radio) {
            radio.addEventListener('change', function () {
                document.querySelectorAll('.ca-role-option').forEach(function (item) {
                    item.classList.remove('is-selected');
                });

                if (radio.checked) {
                    radio.closest('.ca-role-option')?.classList.add('is-selected');
                }
            });
        });
    </script>
}
