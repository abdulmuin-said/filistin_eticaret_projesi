@model FilistinProje.Core.Varliklar.AppUser
@{
    ViewData["Title"] = Localizer["Admin_ChangePassword"];
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";

    var displayName = string.IsNullOrWhiteSpace(Model.AdSoyad) ? Localizer["Admin_UnnamedUser"].Value : Model.AdSoyad;
    var initial = displayName.Substring(0, 1).ToUpperInvariant();
    var email = string.IsNullOrWhiteSpace(Model.Email) ? "-" : Model.Email;
    var phone = string.IsNullOrWhiteSpace(Model.PhoneNumber) ? "-" : Model.PhoneNumber;
    var city = string.IsNullOrWhiteSpace(Model.Sehir) ? "-" : Model.Sehir;
    var isLocked = Model.LockoutEnd.HasValue && Model.LockoutEnd.Value > DateTimeOffset.UtcNow;
}

<style>
    .ca-password-shell {
        max-width: 1120px;
        margin: 0 auto;
    }

    .ca-password-grid {
        display: grid;
        grid-template-columns: minmax(0, 1fr) 340px;
        gap: 18px;
        align-items: start;
    }

    .ca-password-hero {
        display: flex;
        justify-content: space-between;
        gap: 16px;
        align-items: flex-start;
        margin-bottom: 18px;
    }

    .ca-password-user {
        display: flex;
        gap: 14px;
        align-items: center;
        padding: 18px;
        border-bottom: 1px solid var(--ca-border);
    }

    .ca-password-avatar {
        width: 60px;
        height: 60px;
        border-radius: 18px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: var(--ca-primary);
        color: #fff;
        font-size: 1.35rem;
        font-weight: 800;
        flex: 0 0 auto;
    }

    .ca-password-input-wrap {
        position: relative;
    }

    .ca-password-input-wrap .ca-input {
        padding-right: 48px;
    }

    .ca-password-toggle {
        position: absolute;
        right: 10px;
        top: 50%;
        transform: translateY(-50%);
        width: 34px;
        height: 34px;
        border: 0;
        border-radius: 9px;
        background: transparent;
        color: var(--ca-text-muted);
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: .18s ease;
    }

    .ca-password-toggle:hover {
        background: var(--ca-bg);
        color: var(--ca-primary);
    }

    .ca-password-meter {
        height: 8px;
        border-radius: 999px;
        background: var(--ca-bg);
        overflow: hidden;
        margin-top: 10px;
    }

    .ca-password-meter span {
        display: block;
        height: 100%;
        width: 0;
        border-radius: inherit;
        background: var(--ca-danger);
        transition: .2s ease;
    }

    .ca-password-rules {
        margin: 14px 0 0;
        padding: 0;
        list-style: none;
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 9px;
    }

    .ca-password-rules li {
        display: flex;
        align-items: center;
        gap: 8px;
        color: var(--ca-text-muted);
        font-size: .8rem;
    }

    .ca-password-rules i {
        color: var(--ca-border-strong, #c9c2b7);
    }

    .ca-password-rules li.is-ok {
        color: var(--ca-success);
    }

    .ca-password-rules li.is-ok i {
        color: var(--ca-success);
    }

    .ca-summary-row {
        display: flex;
        justify-content: space-between;
        gap: 14px;
        padding: 12px 0;
        border-bottom: 1px solid var(--ca-border);
    }

    .ca-summary-row:last-child {
        border-bottom: 0;
        padding-bottom: 0;
    }

    .ca-summary-row span {
        color: var(--ca-text-muted);
        font-size: .78rem;
    }

    .ca-summary-row strong {
        color: var(--ca-text);
        font-size: .82rem;
        text-align: right;
        word-break: break-word;
    }

    .ca-warning-list {
        margin: 0;
        padding: 0;
        list-style: none;
        display: grid;
        gap: 10px;
    }

    .ca-warning-list li {
        display: flex;
        gap: 9px;
        color: var(--ca-text-muted);
        font-size: .82rem;
        line-height: 1.45;
    }

    .ca-warning-list i {
        color: var(--ca-accent);
        margin-top: 3px;
    }

    @@media (max-width: 992px) {
        .ca-password-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 680px) {
        .ca-password-hero {
            flex-direction: column;
        }

        .ca-password-rules {
            grid-template-columns: 1fr;
        }
    }
</style>

<div class="container-fluid ca-fade-in">
    <div class="ca-password-shell">
        <div class="ca-password-hero">
            <div>
                <div class="ca-page-kicker">@Localizer["Admin_MemberSecurity"]</div>
                <h1 class="ca-page-title">@Localizer["Admin_ChangePassword"]</h1>
                <p class="ca-page-subtitle">@Localizer["Admin_ChangePasswordSubtitle"]</p>
            </div>
            <div class="d-flex gap-2 flex-wrap justify-content-end">
                <a href="/Admin/Kullanici/Duzenle/@Model.Id" class="ca-btn ca-btn-outline">
                    <i class="fas fa-user-pen"></i> @Localizer["Admin_EditUser"]
                </a>
                <a href="/Admin/Kullanici" class="ca-btn ca-btn-outline">
                    <i class="fas fa-arrow-left"></i> @Localizer["Admin_BackToList"]
                </a>
            </div>
        </div>

        <div class="ca-password-grid">
            <form method="post" class="ca-card" id="passwordForm">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" value="@Model.Id" />

                <div class="ca-card-header">
                    <h2 class="ca-card-title mb-0">@Localizer["Admin_NewPasswordInformation"]</h2>
                </div>

                <div class="ca-card-body">
                    @if (TempData["Hata"] != null)
                    {
                        <div class="ca-alert ca-alert-danger mb-3">
                            <i class="fas fa-triangle-exclamation"></i>
                            <span>@TempData["Hata"]</span>
                        </div>
                    }

                    <div class="ca-alert ca-alert-warning mb-4">
                        <i class="fas fa-triangle-exclamation"></i>
                        <span>@Localizer["Admin_PasswordResetWarning"]</span>
                    </div>

                    <div class="row g-3">
                        <div class="col-12">
                            <label class="ca-label" for="yeniSifre">@Localizer["Admin_NewPassword"] <span class="required">*</span></label>
                            <div class="ca-password-input-wrap">
                                <div class="ca-input-icon-group">
                                    <i class="fas fa-lock ca-icon-left"></i>
                                    <input type="password"
                                           id="yeniSifre"
                                           name="yeniSifre"
                                           class="ca-input"
                                           required
                                           minlength="6"
                                           autocomplete="new-password"
                                           placeholder='@Localizer["Admin_PasswordExamplePlaceholder"]' />
                                </div>
                                <button type="button" class="ca-password-toggle" data-toggle-password="yeniSifre" aria-label="@Localizer["Admin_TogglePasswordVisibility"]">
                                    <i class="fas fa-eye"></i>
                                </button>
                            </div>
                            <div class="ca-password-meter" aria-hidden="true"><span id="passwordMeter"></span></div>
                            <small class="ca-field-help" id="passwordHint">@Localizer["Admin_StrongPasswordHint"]</small>
                        </div>

                        <div class="col-12">
                            <label class="ca-label" for="sifreTekrar">@Localizer["Admin_ConfirmNewPassword"] <span class="required">*</span></label>
                            <div class="ca-password-input-wrap">
                                <div class="ca-input-icon-group">
                                    <i class="fas fa-check-double ca-icon-left"></i>
                                    <input type="password"
                                           id="sifreTekrar"
                                           name="sifreTekrar"
                                           class="ca-input"
                                           required
                                           minlength="6"
                                           autocomplete="new-password"
                                           placeholder="@Localizer["Admin_RepeatPasswordPlaceholder"]" />
                                </div>
                                <button type="button" class="ca-password-toggle" data-toggle-password="sifreTekrar" aria-label="@Localizer["Admin_TogglePasswordConfirmationVisibility"]">
                                    <i class="fas fa-eye"></i>
                                </button>
                            </div>
                            <small class="ca-field-help" id="matchHint">@Localizer["Admin_PasswordsMustMatch"]</small>
                        </div>
                    </div>

                    <ul class="ca-password-rules" aria-label="@Localizer["Admin_PasswordRules"]">
                        <li data-rule="length"><i class="fas fa-circle-check"></i> @Localizer["Admin_PasswordRuleLength"]</li>
                        <li data-rule="upper"><i class="fas fa-circle-check"></i> @Localizer["Admin_PasswordRuleUppercase"]</li>
                        <li data-rule="lower"><i class="fas fa-circle-check"></i> @Localizer["Admin_PasswordRuleLowercase"]</li>
                        <li data-rule="number"><i class="fas fa-circle-check"></i> @Localizer["Admin_PasswordRuleNumber"]</li>
                        <li data-rule="symbol"><i class="fas fa-circle-check"></i> @Localizer["Admin_PasswordRuleSymbol"]</li>
                        <li data-rule="match"><i class="fas fa-circle-check"></i> @Localizer["Admin_PasswordRuleMatch"]</li>
                    </ul>
                </div>

                <div class="ca-card-footer d-flex justify-content-end gap-2 flex-wrap">
                    <a href="/Admin/Kullanici" class="ca-btn ca-btn-outline">@Localizer["Admin_Iptal"]</a>
                    <button type="submit" class="ca-btn ca-btn-danger" id="submitPassword">
                        <i class="fas fa-key"></i> @Localizer["Admin_UpdatePassword"]
                    </button>
                </div>
            </form>

            <aside>
                <div class="ca-card mb-3">
                    <div class="ca-password-user">
                        <span class="ca-password-avatar">@initial</span>
                        <div class="min-w-0">
                            <h2 class="ca-card-title mb-1">@displayName</h2>
                            <div class="small text-muted text-break">@email</div>
                            <div class="mt-2">
                                @if (isLocked)
                                {
                                    <span class="ca-badge ca-badge-danger"><i class="fas fa-ban"></i> @Localizer["Admin_LoginBlocked"]</span>
                                }
                                else
                                {
                                    <span class="ca-badge ca-badge-success"><i class="fas fa-check"></i> @Localizer["Admin_ActiveAccount"]</span>
                                }
                            </div>
                        </div>
                    </div>
                    <div class="ca-card-body">
                        <div class="ca-summary-row">
                            <span>@Localizer["Admin_E_posta"]</span>
                            <strong>@email</strong>
                        </div>
                        <div class="ca-summary-row">
                            <span>@Localizer["Admin_Telefon"]</span>
                            <strong>@phone</strong>
                        </div>
                        <div class="ca-summary-row">
                            <span>@Localizer["Admin_Sehir"]</span>
                            <strong>@city</strong>
                        </div>
                    </div>
                </div>

                <div class="ca-card">
                    <div class="ca-card-header">
                        <h2 class="ca-card-title mb-0">@Localizer["Admin_OperationNotes"]</h2>
                    </div>
                    <div class="ca-card-body">
                        <ul class="ca-warning-list">
                            <li>
                                <i class="fas fa-shield-halved"></i>
                                <span>@Localizer["Admin_SharePasswordSecurelyNote"]</span>
                            </li>
                            <li>
                                <i class="fas fa-right-from-bracket"></i>
                                <span>@Localizer["Admin_PasswordClearsSessionNote"]</span>
                            </li>
                            <li>
                                <i class="fas fa-clock-rotate-left"></i>
                                <span>@Localizer["Admin_PasswordAuditNote"]</span>
                            </li>
                        </ul>
                    </div>
                </div>
            </aside>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const passwordInput = document.getElementById('yeniSifre');
        const repeatInput = document.getElementById('sifreTekrar');
        const meter = document.getElementById('passwordMeter');
        const passwordHint = document.getElementById('passwordHint');
        const matchHint = document.getElementById('matchHint');
        const passwordTexts = {
            weak: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Localizer["Admin_PasswordStrengthWeak"].Value)),
            medium: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Localizer["Admin_PasswordStrengthMedium"].Value)),
            strong: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Localizer["Admin_PasswordStrengthStrong"].Value)),
            mustMatch: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Localizer["Admin_PasswordsMustMatch"].Value)),
            matching: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Localizer["Admin_PasswordsMatching"].Value)),
            notMatching: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Localizer["Admin_PasswordsNotMatching"].Value)),
            submitMismatch: @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Localizer["Admin_PasswordSubmitMismatch"].Value))
        };

        function setRule(name, isOk) {
            const item = document.querySelector(`[data-rule="${name}"]`);
            if (item) {
                item.classList.toggle('is-ok', isOk);
            }
        }

        function updatePasswordState() {
            const value = passwordInput.value || '';
            const repeat = repeatInput.value || '';
            const checks = {
                length: value.length >= 6,
                upper: /[A-ZÇĞİÖŞÜ]/.test(value),
                lower: /[a-zçğıöşü]/.test(value),
                number: /\d/.test(value),
                symbol: /[^A-Za-z0-9ÇĞİÖŞÜçğıöşü]/.test(value),
                match: value.length > 0 && value === repeat
            };

            Object.keys(checks).forEach((key) => setRule(key, checks[key]));

            const score = ['length', 'upper', 'lower', 'number', 'symbol'].filter((key) => checks[key]).length;
            const percent = Math.max(8, score * 20);
            meter.style.width = `${percent}%`;
            meter.style.background = score <= 2 ? '#ef4444' : score <= 4 ? '#f59e0b' : '#16a34a';
            passwordHint.textContent = score <= 2
                ? passwordTexts.weak
                : score <= 4
                    ? passwordTexts.medium
                    : passwordTexts.strong;

            if (!repeat) {
                matchHint.textContent = passwordTexts.mustMatch;
                matchHint.classList.remove('text-success');
                matchHint.classList.remove('text-danger');
            } else if (checks.match) {
                matchHint.textContent = passwordTexts.matching;
                matchHint.classList.add('text-success');
                matchHint.classList.remove('text-danger');
            } else {
                matchHint.textContent = passwordTexts.notMatching;
                matchHint.classList.add('text-danger');
                matchHint.classList.remove('text-success');
            }
        }

        document.querySelectorAll('[data-toggle-password]').forEach((button) => {
            button.addEventListener('click', () => {
                const input = document.getElementById(button.dataset.togglePassword);
                const icon = button.querySelector('i');
                const shouldShow = input.type === 'password';
                input.type = shouldShow ? 'text' : 'password';
                icon.classList.toggle('fa-eye', !shouldShow);
                icon.classList.toggle('fa-eye-slash', shouldShow);
            });
        });

        passwordInput.addEventListener('input', updatePasswordState);
        repeatInput.addEventListener('input', updatePasswordState);

        document.getElementById('passwordForm').addEventListener('submit', function (event) {
            if (passwordInput.value !== repeatInput.value) {
                event.preventDefault();
                repeatInput.focus();
                matchHint.textContent = passwordTexts.submitMismatch;
                matchHint.classList.add('text-danger');
            }
        });

        updatePasswordState();
    </script>
}
